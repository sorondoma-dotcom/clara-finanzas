{
  "name": "gestion-de-finanzas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/finance.test.js",
    "test:browser": "node tests/accounts-browser.mjs",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:test": "docker compose --profile test run --rm --build tests",
    "db:up": "docker compose up -d db"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Clara · planificador de gastos personales (frontend React). La API vive en backend/ (FastAPI + PostgreSQL).",
  "dependencies": {
    "lucide-react": "^1.42.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "recharts": "^3.10.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "^6.4.3"
  }
}
